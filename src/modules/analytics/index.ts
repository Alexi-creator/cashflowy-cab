/** Public entry of the module: types and domain schemas.
 *  Components come from `@/modules/analytics/ui`, every other segment by an explicit path. */
export type { ExpensesSummary, IncomesSummary, SummaryGranularity } from "./model"
