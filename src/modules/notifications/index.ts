/** Public entry of the module: types and domain schemas.
 *  Components come from `@/modules/notifications/ui`, every other segment by an explicit path. */
export type { BotNotificationPreference } from "./model"
