/** Public entry of the module: types and domain schemas.
 *  Components come from `@/modules/categories/ui`, every other segment by an explicit path. */

export type { Category, CategoryStats } from "./model"
export { categorySchema } from "./model"
